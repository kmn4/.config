# conf.d/ のスクリプトは config.fish より前にロードされる。
# completions/ にはエンドユーザレベルの補完情報を追加する。
# どちらにも実際は外部パッケージのファイルくらいしか置かないので .gitignore する。
/conf.d/
/completions/
# マシン固有の追加的な設定を書く。config.fish の最後に評価される。
extra-config.fish
# ユニバーサル変数が保存されるファイル。"Do not edit this file directly"
fish_variables
